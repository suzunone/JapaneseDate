## 不具合修正の手順

不具合を修正する場合は、必ず以下の順序で作業する。

1. 不具合を再現するテストを追加または修正する
2. 実装を変更する前にそのテストを実行し、期待した原因で失敗することを確認する
3. 不具合の実装を修正する
4. 追加・修正したテストを再実行し、成功することを確認する
5. 影響範囲に応じて関連テストおよび全体テストを実行する

実装変更前の失敗確認を省略してはならない。環境依存などの理由で不具合を再現できない場合は、実装修正へ進む前に再現できない理由と確認した内容をユーザーへ報告する。
